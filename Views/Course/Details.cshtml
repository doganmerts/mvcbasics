@model Course 
@await Html.PartialAsync("_Course", @Model)



<!-- <main>
  <div class="container">
    <h1>Course List</h1>
    <div class="row">
      <div class="col-md-4">
        <img
          src="~/img/@Model.Image"
          alt=""
          width="200"
          class="img-fluid-rounded-start"
        />
      </div>
      <div class="col-md-8">
        <h1 class="card-title">@Model.Title</h1>
        <p class="card-text">@Model.Description</p>
      </div>
    </div>
  </div>
</main>
 -->
<!-- <h1>Courses Homepage</h1>
        <div class="card">
        <div class="row">
          <div class="col-md-4"><img src="~/img/4.jpg" alt=""  width="200" class="img-fluid-rounded-start" /></div>
          <div class="col-md-8">
            <p>@Model.Id</p>
            <p class="card-title">@Model.Title</p>
            <p class="card-text">@Model.Description</p>
          </div>
        </div>
      </main>
    </div> -->
